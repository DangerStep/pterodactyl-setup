# How to set up mc server with hpiss elitedesk computer

## STEP 1: Install Debian

Go to this website and download debian-13.x.x-amd64-netinst.iso
https://www.debian.org/download

Then, go here and install the latest version of rufus (doesn't matter if its the portable version)
https://rufus.ie/en/

Grab your PUDIDMS (Portable USB Drive to Install Debian for a Minecraft Server) and plug it into your computer. Open rufus, and select the usb drive in the top under "Device". Press SELECT and select the Debian iso, and press install. If it asks about it being a hybrid iso, use the recommended option.

Make sure that your computer has a drive in it. In your specific case, you SHOULD be able to just plug in the usb to the computer and turn it on, it should open the Debian installer. If it doesn't do that, try turning on and spamming either ESC or F9. If neither of those work, ping me on discord. Select GUI installer, and it should open to a menu that says select a language, if you got to this point, you are doing very good.

## STEP 2: Install Debian 2

Once it opens to the select language menu, select the default values for every prompt EXCEPT for the ones listed below. If it is not listed below, just press enter.


Hostname, full name for the new user, username for your account, password for account, time zone: anything: anything you want

Root Password: make a SECURE PASSWORD (please im begging you)

Partition disks: this is a very important section. Select these EXACT options:

Guided - use entire disk

the MAIN DRIVE you want to install Debian onto, usually the largest one

ALL FILES IN ONE PARTITION (this is the most important one)

Finish partitioning and write changes to disk.

Software Selection: only select standard system utilities and SSH server
	
Install GRUB bootloader: yes

Once it asks, reboot your system


## OPTIONAL: Use SSH for a terminal
If you don't want to manually type in all of the commands into the terminal, you can do this:
On the hpiss computer: run the command ``hostname -I`` and look at the FIRST ip address (WRITE THIS DOWN YOU WILL USE IT A LOT, IT IS YOUR LOCAL IP)
On your pc, open "cmd" and run this command: ``ssh root@[IP from the last command]``. Enter in "Yes". Now enter the root password that you used to login to your computer.

You now have access to the pc through your pc. Use right click in the terminal to paste, control+v doesn't work.

## OPTIONAL: Secure your server
If you would like to set up a firewall on your server, run these commands:
```
sudo apt install -y ufw

sudo ufw default deny incoming
sudo ufw default allow outgoing

sudo ufw allow 22/tcp
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw allow 25565/tcp
sudo ufw allow 8080/tcp
sudo ufw allow 8100/tcp
sudo ufw allow 19132/udp

sudo ufw enable
sudo ufw status
```


## STEP 3: Install Programs
Once the computer reboots, login using the username you made and the password you made. Then, run these commands to update the system:
```
sudo apt update
sudo apt upgrade -y

sudo apt install -y curl wget git unzip
```

### Install docker

```
curl -sSL https://get.docker.com | sh
sudo systemctl enable docker
sudo systemctl start docker

sudo usermod -aG docker $USER
```

Now reboot your computer, and log in again.

### Install dependencies and Docker Compose

```
sudo apt install -y nginx mariadb-server redis-server php php-cli php-fpm php-mysql php-zip php-gd php-mbstring php-curl php-xml php-bcmath unzip

curl -sS https://getcomposer.org/installer | php
sudo mv composer.phar /usr/local/bin/composer
```

### Download Pterodactyl Wings & Panel (server manager & web gui)

Panel first
```
sudo mkdir -p /var/www/pterodactyl
sudo chown -R $USER:$USER /var/www/pterodactyl
cd /var/www/pterodactyl

sudo curl -Lo panel.tar.gz https://github.com/pterodactyl/panel/releases/latest/download/panel.tar.gz
sudo tar -xzvf panel.tar.gz
sudo chmod -R 755 storage/* bootstrap/cache/

cp .env.example .env
composer install --no-dev --optimize-autoloader
php artisan key:generate --force
```

Now wings
```
sudo mkdir -p /etc/pterodactyl
cd /etc/pterodactyl

sudo curl -L -o wings https://github.com/pterodactyl/wings/releases/latest/download/wings_linux_amd64
sudo chmod +x wings

sudo nano /etc/systemd/system/wings.service
```

Paste this:
```
[Unit]
Description=Pterodactyl Wings Daemon
After=docker.service
Requires=docker.service

[Service]
User=root
WorkingDirectory=/etc/pterodactyl
LimitNOFILE=4096
ExecStart=/etc/pterodactyl/wings
Restart=always

[Install]
WantedBy=multi-user.target
```

And run this:
```
sudo systemctl daemon-reexec
sudo systemctl daemon-reload

sudo systemctl enable wings
sudo systemctl start wings
```

### Database Setup

``sudo mysql -u root -p``
Then run
```
CREATE DATABASE panel;
CREATE USER 'ptero'@'127.0.0.1' IDENTIFIED BY 'strongpassword';
GRANT ALL PRIVILEGES ON panel.* TO 'ptero'@'127.0.0.1';
FLUSH PRIVILEGES;
EXIT;
```

### Run Installer
Run ``ip a`` and look for ``inet`` and take note of the ip address that comes after it

```
php artisan p:environment:setup
php artisan p:environment:database
php artisan migrate --seed --force
php artisan p:user:make
```

1. Egg Author Email: enter in your email
2. Application URL: use the ip address you took note of earlier
3. Application Timezone: look at reference link
4. Cache Driver: default
5. Session Driver: default
6. Queue Driver: default
7. Enable UI based settings editor: default
8. Enable sending anonymous telemetry data: you pick
9. Redis host, password, and port: default
10. Database host, port and name: default
11. Database Username: ptero
12. Database Password: strongpassword
13. Is this user an admin: yes
14. Email: your email
15. Username: you pick
16. First, Last name: your first and last name
17. Password: create a password


### Configure Nginx (web server)
``sudo nano /etc/nginx/sites-available/pterodactyl``

Then paste:
```
server {
    listen 80;
    server_name _;

    root /var/www/pterodactyl/public;
    index index.php;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        fastcgi_pass unix:/run/php/php8.4-fpm.sock;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
    }
}
```
Control+x, y, then enter to save
Enable:
```
sudo ln -s /etc/nginx/sites-available/pterodactyl /etc/nginx/sites-enabled/
sudo systemctl restart nginx
```

## !! If you go to the local ip that you noted earlier, and you get the default apache page, or any error, follow these instructions:

nginx is probably serving `/var/www/html` instead of Pterodactyl. Disable the default nginx site and make the Pterodactyl site the default:

```
sudo rm -f /etc/nginx/sites-enabled/default
sudo rm -f /etc/nginx/sites-available/default
sudo nano /etc/nginx/sites-available/pterodactyl
```

Delete everything, and use this config:
```
server {
    listen 80 default_server;
    listen [::]:80 default_server;

    server_name _;

    root /var/www/pterodactyl/public;
    index index.php index.html;

    location / {
        try_files $uri /index.php?$query_string;
    }

    location ~ \.php$ {
        include fastcgi_params;
        fastcgi_pass unix:/run/php/php8.4-fpm.sock;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
    }
}
```

ping me if anything goes wrong

## Step 4: Set up the everything but the server

Open the panel in your browser:
```http://[your server ip]```
Login with the admin account you created earlier

### Create a node
Go to:

Admin (settings button in top right)
Locations
Create New

Type in a name like "local machine"

Then go to:
Nodes
Create new

and enter in these values (if something isn't mentioned leave it at the default value):

1. enter in a name and description
3. for the FQDN, use your machine's local ip from earlier
4. select "Use HTTP connection"
5. set total memory to a little less than your computers total ram (prefeered 1-2GB for system overhead. For example, if you had 16GiB, look up like 15-14GiB to MiB (the i is important))
6. memory over-allocation set to -1
7. set total disk space to 51200
8. disk over-allocation is also -1

### Configuration
Go to "Assign New Allocations" and type in:
IP Address: your local ip from earlier
Alias: mc server or smth idk
ports: 25565 (required) and 8100 (only if using pl3xmap)

then press submit

### Add your node to the backend
Go to Configuration, and copy the entire config

Then, go to the server:
```sudo nano /etc/pterodactyl/config.yml
```
and paste the whole config file

Then run:
```
sudo systemctl restart wings
sudo systemctl status wings
```

You want to see "active (running)"

Go back to the panel, and go to the nodes tab, you want to see your node say ``Online`` and show a green heart

if anything goes wrong ping me

## Step 5: Set up the server

Go to the admin area (settings button in top right)
Go to:
Servers
Create New

and enter in these values (if something isn't mentioned leave it at the default value):

1. Server Name: minecraft (or whatever you want, this is not a college essay)
2. Server Owner: enter your admin user email
3. Node: pick the node you made earlier
4. Default Allocation: pick the allocation with 25565

Scroll down to Application Feature Limits:
1. CPU Limit: 0
2. Memory: 4096 (this is 4GB of RAM. If you have more players or plugins, use 6144 or 8192. Do not give it all your RAM, the OS needs some too)
3. Swap: 0
4. Disk Space: 20000

Scroll down to Nest Configuration:
1. Nest: Minecraft
2. Egg: Paper (best pick for normal survival. if doing Forge or Fabric mods, pick those eggs instead. don't pick Paper and wonder why Forge doesn't load)

Scroll down to Docker Configuration and leave it alone, it should pick the right Java image for you.

Scroll down to Startup Configuration:
1. Minecraft Version: latest
2. Build Number: latest
3. Server Jar File: server.jar

Press Create Server

Wait for the install to finish. If it says installing, let it cook. If it screams in red text, read the error instead of pretending it is decorative or ping me on discord.

## Step 6: Start the server once

Go back to the normal user side of the panel.
Open your server.

Click Start

The first start will stop and complain about the EULA. That is normal.

Go to:
Files
Click on eula.txt

Change this:
``eula=false``

to this:
``eula=true``

Save it.

Go back to the console and start the server again. Congrats, you have agreed to the block game paperwork.

## Step 7: Open the port & BGW320 Port Forwarding

If you used UFW earlier to secure your server, make sure the port is allowed. Run these commands in your hpiss SSH terminal:
```
sudo ufw allow 25565/tcp
sudo ufw reload
sudo ufw status
```

If you want your friends to join from outside your house, you have to port forward your router. Here is how to do it for the AT&T BGW320:

Open your browser and go here:
```http://192.168.1.254```

Go to:
Firewall
NAT/Gaming

It will ask you for a Device Access Code. Look at the sticker on the back or bottom of your BGW320 router, type in the code it shows.

Click Custom Services
Then click Add

Enter in these exact options:
1. Service Name: mc server or smth idk
2. Global Port Range: 25565 - 25565
3. Base Host Port: 25565
4. Protocol: TCP/UDP

Click Add.

Then click Return to NAT/Gaming

Under Application Hosting Entry:
1. Application: select the "mc server" service you just made
2. Needed by Device: select your hpiss computer's local ip from the dropdown list

Click Add.

You are done! Give your friends your public ip address (just google "what is my ip" on your pc) and they can join. ping me if anything goes wrong
